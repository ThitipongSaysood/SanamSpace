/.agents/private/

# Next.js app
frontend/node_modules/
frontend/.next/

# root compose env (DB creds)
/.env

# macOS Finder droppings
.DS_Store

# Security scan output. The skill itself lives in its own repo (~/claude-skills)
# so it can be shared; only its findings ever land here, and findings are
# regenerable output that should not be committed.
/Security_Report_*.html
/Security_Report_*.xlsx
/scan-*.json
/*.skill
