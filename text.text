
เริ่ม session:
1. อ่าน .agents/AGENTS.md
2. อ่าน .agents/active.md
3. สรุปกติกาสำคัญ + สถานะล่าสุด + next steps ก่อนลงมือ

ระหว่างทำงาน:
- ไม่ต้อง log อะไร แค่ทำงาน

จบ session:
1. อัปเดต .agents/active.md
   - Current Task
   - Status
   - What's Done
   - Blockers
   - Next Steps
   - Last Updated
   - Last Agent
2. อัปเดต .agents/AGENTS.md เฉพาะส่วนที่เป็นกติกาถาวรหรือ Last Agent ถ้าจำเป็น
3. สร้างไฟล์ checkpoint ใหม่ใน .agents/sessions/YYYY-MM-DD-{topic}.md
4. สรุปไฟล์ที่แก้ + สิ่งที่ต้องทำต่อ


เริ่ม session: อ่าน .agents/AGENTS.md และ .agents/active.md แล้วสรุปสถานะล่าสุด + next steps ก่อนลงมือ

จบ session: อัปเดต .agents/active.md, อัปเดต .agents/AGENTS.md ถ้ามีกติกาถาวรเปลี่ยน, สร้าง .agents/sessions/YYYY-MM-DD-{topic}.md แล้วสรุปสิ่งที่ทำ/ค้าง/ต่อไป

ทำ session close-out (อัพเดต active.md + สร้างไฟล์ใน sessions/) ปิดวัน 
Commit + push


รัน backend
cd backend && php artisan serve

cd backend && php artisan serve          
cd frontend && npm run dev               



/owner    เจ้าของสนาม — owner@everyday.test / password
/admin    แพลตฟอร์ม — super@sanamspace.test / password



 Customer App — ขาดอะไร
ปุ่มมีแต่ยังไม่ทำงาน (quick win):

💰 เติมเงิน wallet — ปุ่มมี ไม่มี handler/endpoint
🎫 ซื้อแพ็กเกจ — ปุ่ม "ซื้อเลย" ยังไม่ทำงาน
⭐ รีวิว/ให้ดาว — ดูได้ แต่ลูกค้าเขียนรีวิวไม่ได้
ยังไม่มีเลย:

🔑 LINE Login จริง — ยัง mock (auth-context.tsx hardcode "คุณสมชาย"), backend มี TODO ยังไม่ verify token
สมัครสมาชิก / login เบอร์-อีเมล (ปุ่ม disabled)
เลื่อนการจอง (reschedule), โค้ดส่วนลด, รายการโปรด, ตั้งค่าแจ้งเตือน (toggle เป็น local อย่างเดียว)
⚠️ ป้าย "(เดโม่)" ที่ปุ่มเช็คอิน

🏢 Owner Portal — ขาดอะไร
อ่านอย่างเดียว ทั้งที่ควรแก้ได้:

👥 ลูกค้า — ดู list ได้ ไม่มีหน้า detail/แก้ไข/โน้ต
🧑‍💼 พนักงาน — เชิญได้อย่างเดียว แก้/ลบ/ระงับ/เปลี่ยน role ไม่ได้ (backend ไม่มี endpoint)
🎟️ สมาชิก — ปรับแต้มได้ แต่จัดการ tier/วันหมดอายุไม่ได้
ยังไม่มีเลย:

💸 คืนเงิน (refund) — สำคัญมาก
🔧 ปิดคอร์ทซ่อมบำรุง / บล็อกวันหยุด
💲 ราคาตามช่วงเวลา (peak pricing), จองแบบ recurring
📊 export รายงาน (CSV/PDF) — ตอนนี้ดูได้อย่างเดียว
📢 รับประกาศจาก platform (ที่เพิ่งทำฝั่ง admin ยังไม่มีฝั่ง owner)
Mock/hardcode ที่เหลือ: ตัวเลข % เทียบเมื่อวานบน dashboard (hardcode), search bar บน topbar, badge แจ้งเตือน "3", แท็บ Settings "ช่องทางการชำระเงิน" (coming soon)

🛡️ Admin Platform — ขาดอะไร
อ่านอย่างเดียว ทั้งที่ควร action ได้ (7 เมนู):

🆘 ศูนย์ช่วยเหลือ — ดู ticket ได้ แต่ ตอบ/มอบหมาย/ปิดงานไม่ได้ (จุดเจ็บสุด)
👤 ผู้ใช้งานระบบ — ปุ่ม "เพิ่ม" disabled "เร็วๆ นี้"
🔐 บทบาท/สิทธิ์ — แก้ permission ไม่ได้
📅 การสมัครใช้งาน — ต่ออายุ/ยกเลิก/ขยาย trial ไม่ได้
💳 ธุรกรรม/การชำระเงิน — refund/approve ไม่ได้
🏷️ ฟีเจอร์ — สร้าง/แก้ feature ไม่ได้ (matrix อย่างเดียว)
📜 System Logs — กรอง/export ไม่ได้
ทำงานเต็ม: Dashboard, จัดการสนาม, แพ็กเกจ+ฟีเจอร์, ใบแจ้งหนี้, ประกาศ, ตั้งค่า, สำรองข้อมูล

มีแต่ยังไม่ enforce: 2FA (เก็บค่าแล้วแต่ login ยังไม่บังคับ), auto-backup รายวัน/สัปดาห์ (เก็บนโยบายแล้วแต่ยังไม่มี cron), org drawer แท็บ "ผู้ใช้/ประวัติ" ยังเป็น "กำลังพัฒนา"

